apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  generation: 1
  labels:
    app: ${APPLICATION_NAME}
  name: qa-base-image
  namespace: ${NAMESPACE}
  selfLink: /apis/image.openshift.io/v1/namespaces/${NAMESPACE}/imagestreams/qa-base-image
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: 'docker-registry.default.svc:5000/${NAMESPACE}/qa-base-image'
